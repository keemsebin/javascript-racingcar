export const RACE_MOVE = Object.freeze({
  THRESHOLD: 4,
  FORWARD_STR: "-",
});

export const CAR_NAME = Object.freeze({
  MAX_LENGTH: 5,
});

export const CAR_PROPERTY = Object.freeze({
  NAME: "name",
  POSITION: "position",
});

export const TRY_COUNT = Object.freeze({
  MIN_NUM: 0,
});

export const DELIMITER = ",";
